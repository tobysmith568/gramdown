---
title: Browser converter
description: "Convert a Grammarly .docx to Markdown in your browser: how it runs without an upload, where files are kept, and how to clear them."
order: 4
---

Every page on this site can convert a Grammarly `.docx` **in your browser**. Drop
one anywhere on the page, or use the file picker. Nothing is uploaded.

## Where your files are stored

All files are kept locally on your machine. They are never uploaded anywhere.

## Clearing everything

Remove files one at a time with the delete button on each row. To wipe the whole
store and the saved preference, clear this site's data in your browser's normal
site-data controls.

## Guessing code languages

Grammarly records no language for a code block, so the converter can add a
**guessed** one. The **Guess code languages** toggle in the panel is on by
default here (it is off in the CLI). Flipping it re-labels the files already
converted (no need to drop them again) and updates what **Copy** and **Download**
produce. The guesses still need a human check.
